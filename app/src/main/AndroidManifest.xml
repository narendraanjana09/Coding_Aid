<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nsa.CodingAid">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/appIdForAds"/>
        <service
            android:name="com.nsa.CodingAid.ExtraClasses.BackgroundService"
            android:stopWithTask="false" >
        </service>
        <activity android:name="com.nsa.CodingAid.helperActivity"></activity>
        <activity android:name="com.nsa.CodingAid.needHelpActivity" />
        <activity android:name="com.nsa.CodingAid.WelcomeActivity" />
        <activity android:name="com.nsa.CodingAid.MainActivity"
            android:launchMode="singleTask"
            />
        <activity android:name="com.nsa.CodingAid.infoActivity" />
        <activity android:name="com.nsa.CodingAid.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service android:name="com.nsa.CodingAid.ExtraClasses.FirebaseMessageReceiver">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>